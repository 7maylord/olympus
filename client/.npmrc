# Prevent arbitrary code execution during installation
ignore-scripts=true
# Prevent installation of incompatible peer dependencies
strict-peer-dependencies=false
# Enforce lockfile integrity
prefer-frozen-lockfile=true
